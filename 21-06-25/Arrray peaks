def count_peaks(arr):
    count = 0
    for i in range(1, len(arr) - 1):
        if arr[i] > arr[i - 1] and arr[i] > arr[i + 1]:
            count += 1
    return count

# Example
print(count_peaks([1, 3, 2, 4, 1, 5, 2]))
